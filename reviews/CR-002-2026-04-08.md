# Code Review CR-002 | 2026-04-08

**Preset:** ts | **Scope:** --commits 1 (e2e dedup tests) | **Files reviewed:** 1 | **Verdict:** Needs Attention

---

## Requires Fix (Critical / High)

*None*

---

## Recommendations (Medium)

### 1. [Bug Hunter / Test Quality] Test 1 does not verify duplicate was rejected at data layer
- **File:** `apps/trench/test/e2e/dedup.e2e.test.ts:27-41`
- **Impact:** MEDIUM | **Effort:** LOW
- **Problem:** The first test only checks the API response shape after the duplicate POST but never queries the data layer to confirm only one event was persisted. Tests 2 and 3 both use `waitForQueryResults` to verify persistence, but test 1 skips this step. Test would pass even if dedup returned empty response but still inserted the duplicate.
- **How to fix:** After second POST assertion block, add `waitForQueryResults` to verify exactly 1 event with that uuid exists.
- **Status:** Fixed in commit `5812b92`

---

## Notes (Low)

### 1. [Bug Hunter / Test Quality] Missing test for intra-batch deduplication
- **File:** `apps/trench/test/e2e/dedup.e2e.test.ts`
- **Problem:** No test sends two events with the same `messageId` in a single POST body. This is a distinct code path from the cross-request dedup tested in test 1.
- **How to fix:** Add a test case sending `[{messageId: X}, {messageId: X}]` in single POST and assert `results.length === 1`.

---

## Rejected Findings

| # | Agent | Title | Rejection Reason |
|---|-------|-------|-----------------|
| 1 | Bug Hunter | Implicit reliance on default PRIVATE_API_KEY | Standard pattern across all e2e tests; not a finding |
| 2 | Bug Hunter | In-memory dedup cache assumption | Architectural constraint, not a test code issue |
| 3 | Error Flow | No error handling around authenticatedPost | Tests already assert statusCode; supertest doesn't reject on HTTP errors |
| 4 | Error Flow | Empty test bodies pass vacuously | FALSE POSITIVE — tests 2 and 3 have full implementations |
| 5 | Error Flow | No timeout/error guard around waitForQueryResults | waitForQueryResults already has 10-second timeout with descriptive error |
| 6 | TypeScript | res.body untyped (any) | Universal pattern across all e2e tests; supertest .body is any by design |
| 7 | TypeScript | results[0] without existence guard | Preceded by toHaveLength(1) assertion which guards the access |
| 8 | TypeScript | total: number \| null not handled | POST endpoint always returns total as number, never null |
| 9 | Test Quality | Singleton cache isolation | Using uuidv4() for collision avoidance is robust (1 in 2^122) |

---

## All Clear
- Security — no new issues in diff
- Performance — test file, not production code
- Architecture — file placement, imports, patterns all consistent

---

## Verdict: Needs Attention

1 Medium finding (data-layer verification in test 1) — auto-fixed in commit `5812b92`. 1 Low finding (intra-batch dedup test) is non-blocking. Code follows project conventions well.
