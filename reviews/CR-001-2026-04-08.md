# Code Review CR-001 | 2026-04-08

**Preset:** ts | **Scope:** --commits 5 (event deduplication batch) | **Files reviewed:** 4 | **Verdict:** Needs Attention

---

## Requires Fix (Critical / High)

### 1. [Error Flow] Unawaited `produceEvents` causes silent failures and unrecoverable dedup cache corruption
- **File:** `events.dao.ts:176`
- **Severity:** High
- **Problem:** `produceEvents` is async but called without `await`. The dedup cache records `messageId` before the produce call (line 149). If Kafka produce fails, the event is lost and the client cannot retry within 60s because the cache blocks it.
- **How to fix:** Add `.catch()` with cache rollback for failed messageIds, preserving the fire-and-forget pattern.
- **Source agents:** Error Flow, Bug Hunter

---

## Recommendations (Medium)

### 1. [Bug] Empty string `messageId` creates inconsistent behavior
- **File:** `events.dao.ts:147,156`
- **Impact:** MEDIUM | **Effort:** LOW
- **Problem:** Line 147 uses `!dto.messageId` (falsy — empty string bypasses dedup), but line 156 uses `??` (nullish — empty string is used as UUID). Empty-string messageId bypasses dedup AND produces invalid UUID for ClickHouse.
- **How to fix:** Change `??` to `||` on line 156: `const uuid = eventDTO.messageId || uuidv4()`.
- **Source agents:** Bug Hunter

### 2. [Resilience] Unbounded in-memory Map growth
- **File:** `events.dao.ts:15`
- **Impact:** MEDIUM | **Effort:** LOW
- **Problem:** No size cap on `seenMessages` Map. Burst traffic with unique messageIds can grow memory unboundedly. In single-instance deployment, OOM = total service disruption.
- **How to fix:** Add `MAX_SEEN_SIZE` constant (e.g., 100,000). Skip cache insertion when limit reached (fail open — allow duplicates rather than OOM).
- **Source agents:** Security, Performance

---

## Notes (Low)

### 1. [TypeScript] `mapRowToEvent` produces Invalid Date for `parsedAt`
- **File:** `events.dao.ts:178`, `events.util.ts:28`
- **Problem:** Pre-existing: `KafkaEvent` lacks `parsed_at` field, so `mapRowToEvent` returns `parsedAt: Invalid Date`.
- **How to fix:** Add `parsed_at: new Date()` to row construction, or handle undefined in `mapRowToEvent`.

### 2. [Test Quality] Missing cross-workspace dedup test + hardcoded magic numbers
- **File:** `events.dao.test.ts`
- **Problem:** No test for same messageId across different workspaces. TTL test uses hardcoded `120_000` instead of deriving from class constant.

### 3. [Architecture] Cross-workspace dedup cache collision
- **File:** `events.dao.ts:147-151`
- **Problem:** Cache keyed on `messageId` only, not scoped to workspace. Currently single-tenant (Mindly only), but latent defect if multi-workspace used.
- **How to fix:** Key on `${workspace.databaseName}:${dto.messageId}`.

---

## All Clear

- **ClickHouse migration** (`v006_dedup.sql`): Well-structured ReplacingMergeTree migration with correct ORDER BY and MV recreation
- **FINAL keyword**: Correctly applied to all SELECT queries for read-after-write consistency
- **Core dedup filter logic**: Sound implementation — filter before Kafka, cache on first sight
- **UUID assignment**: Correct messageId → uuid passthrough with uuidv4 fallback
- **Map iteration safety**: ECMAScript-compliant deletion during for...of
- **Test coverage**: 6 tests cover main paths well

---

## Verdict: Needs Attention

One High finding (unawaited produceEvents + cache corruption) should be addressed — the fire-and-forget pattern is pre-existing but the dedup cache makes failure consequences worse. Two Medium findings (empty-string inconsistency, unbounded Map) are low-effort fixes. All three are being auto-fixed.
